{
    "type": "object",
    "properties": {
        "service": {
            "type": "string",
            "description": "The service that the URL belongs to."
        },
        "url": {
            "type": "string",
            "description": "The URL."
        },
        "fetched": {
            "type": "boolean",
            "description": "Indicates if the URL has been fetched from nomad or loaded from the config file."
        }
    },
    "required": ["service", "url", "fetched"]
}
